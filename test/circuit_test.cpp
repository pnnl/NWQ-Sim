#include <iostream>
#include "../include/circuit.hpp"

int main()
{
    NWQSim::Circuit circuit;

    // // Perform some operations on the circuit
    // circuit.X(0);
    // circuit.H(1);
    // circuit.RZ(0.5, 2);

    // // Print the circuit
    // std::string circuitStr = circuit.circuitToString();
    // std::cout << "Circuit: " << circuitStr << std::endl;

    return 0;
}